
<h1>Wettpool >>> <%= @pool.title %>  <<< </h1>

<p>gemanaget von  <%= @pool.user.name %></p>

<p>Mittippen:</p>

<% machmit = true %>
<ul>
  <% @pool.players.each do |player| %>
  <li><%= link_to player.name, pool_player_path( @pool, player ) %>
      <% if current_user?( player ) %>
         <% machmit = false %>
      <% end %>
  </li>
  <% end %>
</ul>


<div>
<% if machmit %>
  <%= link_to 'Mach mit! Mittippen!', play_pool_path( @pool ), :class => 'btn-primary' %>
<% else %>
  <%= link_to 'Tipps Bearbeiten', edit_pool_player_path( @pool, current_user() ), :class => 'btn-primary' %>
<% end %>

<%= link_to 'Zurück', pools_path, :class => 'btn' %>
</div>