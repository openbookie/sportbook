<h1><%= Team.count %> Teams</h1>

<table>
 <% @teams.each do |team| %>
   <tr>
     <td>(<%= team.id %>)</td>
     <td>
         <%= image_tag( team.img ) if team.img.present? %>
     </td>
     <td><%= team.title %> (<%= team.key %>)</td>
   </tr>
 <% end %><!-- teams.each -->
</table>
